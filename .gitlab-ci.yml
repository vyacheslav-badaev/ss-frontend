cache:
  paths:
    - node_modules/
before_script:
  - apt-get update -qq && apt-get install -y -qq sshpass
deploy_production:
  stage: deploy
  environment: Production
  only:
    - master
  script:
    - sshpass -V
    - sshpass -p $USER_PASS ssh root@46.101.109.16
    - cd /srv/projects/shortstories-frontend/
    - git pull
    - yarn
    - yarn build:prod
    - pm2 restart all
